package my_practise.p2024.pro.algorithm;

/**
 * projectName:    algorithm
 * package:        my_practise.p2024.pro.algorithm
 * className:      DragonGame
 * author:     BangDi
 * description:  TODO
 * date:    2024/7/25 00:44
 * version:    1.0
 */
public class DragonGame {

    public static int dragon(int[][] matrix) {
        if (matrix == null || matrix.length == 0 || matrix[0] == null || matrix[0].length == 0) {
            return 0;
        }
        int M = matrix.length;
        int N = matrix[0].length;
        int[][] dp = new int[M][N];

        return 0;
    }

}
